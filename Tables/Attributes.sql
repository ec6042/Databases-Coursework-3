CREATE TABLE "Attributes"
  (
     "id"        INTEGER primary key NOT NULL UNIQUE,
     "health"    INTEGER,
     "mana"      INTEGER,
     "energy"    INTEGER,
     "armour"    INTEGER,
     "strength"  INTEGER,
     "agility"   INTEGER,
     "stamina"   INTEGER,
     "intellect" INTEGER,
     "spirit"    INTEGER,
     "xp"        INTEGER
  ); 

